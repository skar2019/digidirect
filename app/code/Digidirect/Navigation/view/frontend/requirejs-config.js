/* eslint no-unused-vars: [1] */
var config = {
    map: {
        '*': {
            'navigation': 'Digidirect_Navigation/js/navigation'
        }
    },
    deps: [
        'babelpolyfill'
    ]
};
