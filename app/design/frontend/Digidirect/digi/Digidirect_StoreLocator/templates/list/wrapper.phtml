<?php
/** @var \Digidirect\Storelocator\Block\Wrapper $block */
$isInPopup = $block->getOpenInPopup();
?>
<div class="store-locator test3">
    <?php echo $block->getChildHtml('storelocator.search'); ?>
    <div class="grid-row test4">

        <div class="<?php echo /* @noEscape */ $isInPopup ? 'inpopup' : 'store-wrapper column -left'; ?>">
            <?php echo $block->getChildHtml('storelocator.list'); ?>
        </div>

        <div class="<?php echo /* @noEscape */ $isInPopup ? 'inpopup' : 'store-wrapper column -right'; ?>">
            <!-- <//?php echo $block->getChildHtml('storelocator.list'); ?> -->
            <p>test</p>
               <div id="tabsection">
                    <ul>
                        <li>
                            <a class="tablinks" href="#tabs1">Tab A</a>
                        </li>
                        <li>
                            <a class="tablinks" href="#tabs2">Tab B</a>
                        </li>
                        <li>
                            <a class="tablinks" href="#tabs3">Tab C</a>
                        </li>
                        <li>
                            <a class="tablinks" href="#tabs4">Tab D</a>
                        </li>
                    </ul>
                    <div id="tabs" class="tabcontent">
                        Content of A
                    </div>
                    <div id="tabs" class="tabcontent">
                        Content of B
                    </div>
                    <div id="tabs" class="tabcontent">
                        Content of C
                    </div>
                    <div id="tabs" class="tabcontent">
                        Content of D
                    </div>

                </div>
        </div>

        <?php if ($isInPopup): ?>
            <?php echo $block->getChildHtml('storelocator.popup'); ?>
        <?php else: ?>
            <div class="google-map-wrapper column -main storemap">
                <?php echo $block->getChildHtml('storelocator.map'); ?>
            </div>
        <?php endif; ?>
    </div>
</div>

<?php echo $block->getChildHtml('storelocator.list.after'); ?>


<script>
    require([
        'jquery',
        'jquery/ui'
    ],
    function ($, tabs) {
        $('#tabsection').tabs();

        var tabID = 0;
        var tabContentID = 0;

        $('tablinks, .tabcontent').each(function(){
        tabContentID++;
        tabID++;

        var tabContentnewID='tabs'+tabContentID;
        var tabnewID='tabs'+tabID;
        $(this).attr('id',tabContentnewID);
        $(this).attr('id',tabnewID);
        });
    });
</script>
