diff --git a/vendor/magento/module-checkout/Model/Session.php b/vendor/magento/module-checkout/Model/Session.php
index 0addbf069cb..ce5af329fb4 100644
--- a/vendor/magento/module-checkout/Model/Session.php
+++ b/vendor/magento/module-checkout/Model/Session.php
@@ -473,6 +473,11 @@ class Session extends \Magento\Framework\Session\SessionManager
      */
     public function clearQuote()
     {
+        \Magento\Framework\Debugger::getInstance()->log(__METHOD__, [
+            'ticket'    => 'ACSD-60016-v2',
+            'getQuoteId'  => $this->getQuoteId(),
+        ]);
+
         $this->_eventManager->dispatch('checkout_quote_destroy', ['quote' => $this->getQuote()]);
         $this->_quote = null;
         $this->setQuoteId(null);
diff --git a/vendor/magento/module-checkout/Model/Session/SuccessValidator.php b/vendor/magento/module-checkout/Model/Session/SuccessValidator.php
index 6bfab606445..7e417378401 100644
--- a/vendor/magento/module-checkout/Model/Session/SuccessValidator.php
+++ b/vendor/magento/module-checkout/Model/Session/SuccessValidator.php
@@ -33,6 +33,14 @@ class SuccessValidator
      */
     public function isValid()
     {
+
+        \Magento\Framework\Debugger::getInstance()->log(__METHOD__, [
+            'ticket'                => 'ACSD-60016-v2',
+            'getLastSuccessQuoteId' => $this->checkoutSession->getLastSuccessQuoteId(),
+            'getLastQuoteId'        => $this->checkoutSession->getLastQuoteId(),
+            'getLastOrderId'        => $this->checkoutSession->getLastOrderId()
+        ]);
+
         if (!$this->checkoutSession->getLastSuccessQuoteId()) {
             return false;
         }
diff --git a/vendor/magento/module-quote/Model/QuoteManagement.php b/vendor/magento/module-quote/Model/QuoteManagement.php
index dc0858f1838..307fd68646a 100644
--- a/vendor/magento/module-quote/Model/QuoteManagement.php
+++ b/vendor/magento/module-quote/Model/QuoteManagement.php
@@ -291,6 +291,13 @@ class QuoteManagement implements CartManagementInterface
         } catch (\Exception $e) {
             throw new CouldNotSaveException(__("The quote can't be created."));
         }
+
+        \Magento\Framework\Debugger::getInstance()->log(__METHOD__, [
+            'ticket'  => 'ACSD-60016-v2',
+            'quoteId' => $quote->getId(),
+            'storeId' => $storeId
+        ]);
+
         return $quote->getId();
     }
 
@@ -309,6 +316,14 @@ class QuoteManagement implements CartManagementInterface
         } catch (\Exception $e) {
             throw new CouldNotSaveException(__("The quote can't be created."));
         }
+
+        \Magento\Framework\Debugger::getInstance()->log(__METHOD__, [
+            'ticket'  => 'ACSD-60016-v2',
+            'quoteId' => $quote->getId(),
+            'customerId' => $customerId,
+            'storeId' => $storeId
+        ]);
+
         return (int)$quote->getId();
     }
 
diff --git a/vendor/magento/module-quote/Model/QuoteRepository.php b/vendor/magento/module-quote/Model/QuoteRepository.php
index b1bef834197..5ddfe0514db 100644
--- a/vendor/magento/module-quote/Model/QuoteRepository.php
+++ b/vendor/magento/module-quote/Model/QuoteRepository.php
@@ -167,6 +167,13 @@ class QuoteRepository implements CartRepositoryInterface
     {
         $quote = $this->get($cartId, $sharedStoreIds);
         if (!$quote->getIsActive()) {
+
+            \Magento\Framework\Debugger::getInstance()->log(__METHOD__, [
+                'ticket'  => 'ACSD-60016-v2',
+                'cartId' => $cartId,
+                'sharedStoreIds' => $sharedStoreIds,
+            ]);
+
             throw NoSuchEntityException::singleField('cartId', $cartId);
         }
         return $quote;
@@ -179,6 +186,13 @@ class QuoteRepository implements CartRepositoryInterface
     {
         $quote = $this->getForCustomer($customerId, $sharedStoreIds);
         if (!$quote->getIsActive()) {
+
+            \Magento\Framework\Debugger::getInstance()->log(__METHOD__, [
+                'ticket'  => 'ACSD-60016-v2',
+                'customerId' => $customerId,
+                'sharedStoreIds' => $sharedStoreIds,
+            ]);
+
             throw NoSuchEntityException::singleField('customerId', $customerId);
         }
         return $quote;
diff --git a/vendor/magento/framework/Debugger.php b/vendor/magento/framework/Debugger.php
new file mode 100644
index 00000000000..ff46fde8cae
--- /dev/null
+++ b/vendor/magento/framework/Debugger.php
@@ -0,0 +1,201 @@
+<?php
+/**
+ * Support Debugger that helps add log messages constantly at separated log file
+ */
+namespace Magento\Framework;
+
+use Magento\Framework\App\Filesystem\DirectoryList;
+use Magento\Framework\App\ObjectManager;
+
+/**
+ * Constant debugger
+ */
+class Debugger
+{
+    /**
+     * Debugger log file name mask
+     */
+    const DEBUGGER_LOG_FILE_MASK = 'debug/support/debug_%s.log';
+
+    /**
+     * Debugger error log file name
+     */
+    const DEBUGGER_ERRORS_LOG_FILE = 'debug_errors.log';
+
+    /**
+     * Store debugger singleton instance
+     *
+     * @var \Magento\Framework\Debugger
+     */
+    private static $_instance = null;
+
+    /**
+     * @var string
+     */
+    private $dir;
+
+    /**
+     * @var string
+     */
+    private $debugFile;
+
+    /**
+     * @var array
+     */
+    private $flags = [];
+
+    /**
+     * Fields that should be replaced in debug data with '***'
+     *
+     * @var array
+     */
+    protected $_debugReplacePrivateDataKeys = array(
+        'cvv', 'cc_cid', 'cc_number', 'customer_password', 'confirm_password', 'last4'
+    );
+
+    /**
+     * Create log directory if it not exists
+     * Prevent creating any instances outside of class because of using singleton pattern
+     *
+     * @param string $logId
+     */
+    private function __construct($logId)
+    {
+        if (empty($logId)) {
+            $logId = session_id() ? session_id() : uniqid();
+        }
+        $filesystem = ObjectManager::getInstance()->get(Filesystem::class);
+        $this->dir = $filesystem->getDirectoryWrite(DirectoryList::VAR_DIR);
+        $this->debugFile = sprintf(self::DEBUGGER_LOG_FILE_MASK, $logId);
+    }
+
+    /**
+     * Prevent creating any clones because of using singleton pattern
+     */
+    private function __clone()
+    {
+    }
+
+    /**
+     * Retrieve singleton instance of debugger
+     *
+     * @param int $logId
+     * @return \Magento\Framework\Debugger
+     */
+    public static function getInstance($logId = null)
+    {
+        if (!(self::$_instance instanceof Debugger)) {
+            self::$_instance = new self($logId);
+        }
+        return self::$_instance;
+    }
+
+    /**
+     * Log message and data
+     *
+     * @param string $message
+     * @param mixed $data
+     */
+    public function log($message, $data = '!empty!', $trace = true)
+    {
+        $stream = $this->dir->openFile($this->debugFile, 'a');
+        $stream->lock();
+        $stream->write($this->prepareMessage($message, $data, $trace));
+        $stream->unlock();
+        $stream->close();
+    }
+
+    /**
+     * Sets flags to keep some data between calls.
+     *
+     * @param string $name
+     * @param mixed $value
+     */
+    public function setFlag($name, $value)
+    {
+        $this->flags[$name] = $value;
+    }
+
+    /**
+     * Gets flag value.
+     *
+     * @param string $name
+     *
+     * @return mixed
+     */
+    public function getFlag($name)
+    {
+        return array_key_exists($name, $this->flags) ? $this->flags[$name] : null;
+    }
+
+    /**
+     * Prepare message and converts data into sting
+     *
+     * @param $message
+     * @param $data
+     *
+     * @return string
+     */
+    private function prepareMessage($message, $data, $trace)
+    {
+        $backtrace = '';
+        $result = '';
+        if ($trace) {
+            if ($data != '!empty!') {
+                $data = $this->_filterDebugData($data);
+                ob_start();
+                var_dump($data);
+                $data = ob_get_clean();
+            }
+            $backtrace = Debug::backtrace(true, false, false);
+            $result = sprintf($this->getMessageTemplate(),
+                date('r'), $message, memory_get_usage(true), memory_get_usage(), $backtrace, $data);
+        } else {
+            $data = json_encode($data, JSON_PRESERVE_ZERO_FRACTION|JSON_UNESCAPED_UNICODE);
+            $result = date('r') . ': ' . $message . ': '. $data . PHP_EOL;
+        }
+
+        return $result;
+    }
+
+    /**
+     * Recursive filter data
+     *
+     * @param mixed $data
+     * @return mixed
+     */
+    private function _filterDebugData($data)
+    {
+        if (is_array($data) && is_array($this->_debugReplacePrivateDataKeys)) {
+            foreach ($data as $key => $value) {
+                if (in_array($key, $this->_debugReplacePrivateDataKeys, true)) {
+                    $data[$key] = '****';
+                } else {
+                    if (is_array($data[$key])) {
+                        $data[$key] = $this->_filterDebugData($data[$key]);
+                    }
+                }
+            }
+        }
+        return $data;
+    }
+
+    /**
+     * Get Message Format Template
+     *
+     * @return string
+     */
+    private function getMessageTemplate()
+    {
+        return <<<EOF
+%s: %s
+MEMORY_REAL_USAGE: %s
+MEMORY_EMALLOC_USAGE: %s
+BACKTRACE:
+%s
+DATA:
+%s
+
+EOF;
+    }
+}
diff --git a/vendor/magento/framework/Exception/NoSuchEntityException.php b/vendor/magento/framework/Exception/NoSuchEntityException.php
index f39fef57ed5..aa3b71e8aa7 100644
--- a/vendor/magento/framework/Exception/NoSuchEntityException.php
+++ b/vendor/magento/framework/Exception/NoSuchEntityException.php
@@ -47,6 +47,11 @@ class NoSuchEntityException extends LocalizedException
      */
     public static function singleField($fieldName, $fieldValue)
     {
+	\Magento\Framework\Debugger::getInstance()->log(__METHOD__, [
+		'ticket' => "ACSD-60016-v2",
+		'fieldName' => $fieldName,
+		'fieldValue' => $fieldValue
+	]);
         return new self(
             new Phrase(
                 'No such entity with %fieldName = %fieldValue',
