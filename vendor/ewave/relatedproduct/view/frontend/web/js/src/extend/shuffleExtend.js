import Shuffle from '../../vendor/shuffle.min';

export default class ShuffleExtend extends Shuffle {
    _validateStyles () {
        return;
    }

    _setTransitions () {
        return;
    }

    _getStylesForTransition () {
        return '';
    }

    _setContainerSize () {
        return;
    }
}
